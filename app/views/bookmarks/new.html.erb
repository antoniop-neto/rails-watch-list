<div class="container">
  <%= link_to '<- Back', list_path(@list) %>
  <h1>Create a new bookmark</h1>
  <%= simple_form_for( [ @list, @bookmark ] ) do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.submit %>
  <% end %>
</div>
